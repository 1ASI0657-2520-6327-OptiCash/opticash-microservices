server:
  port: 8084

spring:
  application:
    name: microservice-contributions
  cloud:
    config:
      enabled: false
  datasource:
    driver-class-name: org.postgresql.Driver
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

services:
  households:
    url: lb://microservice-households
  bills:
    url: lb://microservice-bills
  iam:
    url: lb://microservice-iam

eureka:
  instance:
    hostname: microservice-contributions
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_URI}

documentation:
  application:
    description: "Microservicio de Contributions"
    version: "1.0.0"
